// Generated by AvoCode VERSION 0.0.1, PLEASE EDIT WITH CARE
/* eslint-disable */

var isDev = process.env.NODE_ENV !== "production";

if (typeof __DEV__ !== 'undefined') {
  isDev = __DEV__;
}


if (typeof window !== 'undefined') {
  
  var amplitude = require("avo-amplitude-js");
  var amplitudeDevApiKey = "foo"; 
  var amplitudeProdApiKey = "bar";

}

var asserts;

var setup_ = function(options, systemProps) {
  if (typeof window === 'undefined') {
    return;
  }
  
  if (options.validateProperties !== true) {
    isDev = false;
  }
  
  if (options.useProductionKey === true) {
    amplitude.init(amplitudeProdApiKey);
  } else {
    amplitude.init(amplitudeDevApiKey);
  }
};

var setSystemProps_ = function(systemProps) {
  
};

var gimmeCutestuff = function(gifUrl) {
  if (isDev === true) {
    assertGifUrl(gifUrl);
  }
  
  if (isDev === true) {
    console.log("[avo] Event sent:", "Gimme Cutestuff", "Event props:", {
      "Gif Url": gifUrl});
  }
  
  Amplitude.logEvent("Gimme Cutestuff", {"Gif Url": gifUrl});
};

if (isDev === true) {
  var assertGifUrl = function(gifUrl) {
    asserts.assertString("Gif Url", gifUrl);
  };
}

exports.setup_ = setup_;
exports.setSystemProps_ = setSystemProps_;
exports.gimmeCutestuff = gimmeCutestuff;


if (isDev === true) {
  asserts = require("avo-assert-js");
}
